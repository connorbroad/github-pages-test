export enum DoorLocation {
    North,
    South,
    East,
    West
}

export enum DoorStatus {
    Unlocked,
    Locked,
    Stuck,
}

export enum FloorSize {
    Small,
    Medium,
    Large,
    Mega,
}

export enum SegmentShape {
    Basic,
    Other
}

export enum RoomShape {
    Basic, // rectangle
    Circle,
    Triangle,
    Cross,
    Pentagon,
    Hexagon,
}

export enum CorridorShape {
    Basic, // straight
    U,
    L,
    T,
    S,
    Cross,
}